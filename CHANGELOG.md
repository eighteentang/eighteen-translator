# 更新日志

本文件记录每个发布版本改了什么，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 版本号约定

**`manifest.json` 的 `version` 是单一真源**，另外两处跟着它走：

| 位置 | 规则 |
|---|---|
| `manifest.json` 的 `version` | `x.y.z` 三段（Chrome 不接受四段） |
| git tag | `v` + 同一个版本号（如 `v0.4.1`），打在发布提交上 |
| 本文件 | 顶部第一条就是当前版本，与 tag 同一次提交 |

**三段各代表什么**：

| 段 | 什么时候 +1 | 例子 |
|---|---|---|
| `z` | 修 bug、改文案 | `0.4.0 → 0.4.1` |
| `y` | 加功能 | `0.3.0 → 0.4.0` |
| `x` | 破坏性变更（改配置键、改存储结构） | `0.9.0 → 1.0.0` |

⚠️ **`y` 和 `x` 的区别对升级用户很重要**：`0.4.0` 改过 `targetLang` 的语义并因此写了迁移逻辑，
那正是 `y` 级的例子 —— 老配置会被自动改写成新语义。这类改动必须让人一眼看出来。

⚠️ **本文件顶部版本与 `manifest.json` 不一致时，`dev/check-version.js` 会报错**（CI 里也跑）。
这条约定不靠自律维护，否则第三次发版就会失效。

---

## [0.4.1] - 2026-09-25

首个可发布版本。

> ⚠️ 版本号从 `0.3.0` 直接跳到 `0.4.1` —— 中间的 `0.4.0` **只存在于本地、从未提交**，
> 它的改动全部并入本条。因此下面这些条目实际覆盖 `0.4.0 + 0.4.1` 两个版本的工作。

### 新增

- **完整配置页迭代**（#62）：左侧父级菜单 + 右侧面板，改完不必再拖到页面底部保存；
  保存成功提示改成 2–3 秒自动消失
- **浮层朗读**（#10）：工具条加喇叭图标，只读译文；中文选区时「译成中文」出口改成「朗读原文」。
  走浏览器内置 `speechSynthesis`，离线、不上传、不需要权限。含语速三档与英语美式 / 英式口音
- **自动化验证**：`dev/selftest.js` 扩到 123 项、`dev/e2e-panel.js` 扩到 122 项、
  `dev/scan-i18n.js` 扫描中文硬编码（#39）、CI 每次 push 跑 `check.js` 与 `selftest.js`（#61）

### 变更

- **两个入口的分工重排**：引擎、凭据、翻译方向、主题档位一律进设置页；
  弹层只留高频操作（总开关、主题快捷、语言快捷、去设置、反馈）
- **默认翻译方向由「中文 → 英文」改为「英文 → 中文」**（#2），并修正一处会**改写老用户配置**的迁移 bug：
  老配置里的 `targetLang` 是**输出语言**，原代码把它当成了首选语言 —— 旧默认下两者恰好等价所以没暴露
- **默认界面语言由「全中文」改为「跟随浏览器」**（#35）——
  这是整个改造里**唯一**一处默认行为变更，英文浏览器装上来就是英文界面
- **Google 引擎保留不删**（#5），改为如实标注网络限制：境外或干净出口可用、中国大陆直连不可用、
  被限流的代理稳定 429。默认引擎仍是 DeepSeek
- **`DEFAULTS` 收成单一真源**（#46）：原来有三份拷贝且内容不一致
- **扩展名与商店描述改走 `_locales` + `__MSG_xxx__`**（#56），名字缩短到 45 字符以内

### 修复

- 朗读控件经 3 轮返工：两个中文文字按钮 → 喇叭图标 + 只读译文 → 中文选区时把「译成中文」出口改成「朗读原文」
- 主题快捷按钮与手动模式小圆点改为「描边 + 底色 + 阴影」的图标按钮 ——
  裸图标与背景分不开，用户不知道它可以点
- 浮层字号与最大宽度改为可调（#25），默认档与改造前完全一致

### 移除

- **百度翻译引擎**（v0.3.0 起，含 MD5 签名实现）。如需恢复可从 git 历史里取（#21 / #49）

---

## [0.3.0] - 2026-09-23

### 新增

- **工具栏弹层**（#52）：总开关、主题快捷切换、在此域名不可用、跳转设置页、反馈建议

### 变更

- 挂 **MIT** 协议
- 扩展名与描述本地化（`_locales`，zh_CN / en）

### 移除

- 百度翻译引擎（#21 / #49）

---

## [0.2.0] - 2026-09-23

### 变更

- `DEFAULTS` 收成单一真源（#46）
- 预留「平台免费额度」接缝：`quotaMode` 与 `translateViaPlatform()`（#51）

---

## [0.1.0] - 2026-09-23

### 新增

- 首次入库：Chrome MV3 划词翻译 MVP —— 划词浮层（Shadow DOM）、
  DeepSeek 与 Google 两个引擎可切换、11 种语言互译与本地方向判定、
  排除站点列表、用量与结果缓存、完整设置页
