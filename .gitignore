# 系统垃圾
.DS_Store
Thumbs.db
desktop.ini

# 编辑器 / IDE
.vscode/
.idea/
*.swp

# 打包与签名产物（扩展发布用，不进版本库）
*.zip
*.crx
*.pem
*.key

# 依赖（本项目无 npm，留作兜底）
node_modules/

# 图标脚本的预览产物（--preview 时生成，不必进版本库）
# ⚠️ 写到 dev/ 下而不是 icons/ —— 扩展目录里以 `_` 开头的名字是 Chrome 的保留名
dev/icon-preview/

# WorkBuddy 本地工作数据（项目记忆与归档，不进版本库）
.workbuddy-ai/
